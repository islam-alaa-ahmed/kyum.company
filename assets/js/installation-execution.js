(()=>{
'use strict';
const $=id=>document.getElementById(id);
const esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
const today=()=>new Date().toISOString().slice(0,10);
let rows=[], current=null, activeTab='today', selectedFiles=[];
function setStatus(el,msg,type=''){if(!el)return;el.textContent=msg||'';el.className=`data-status${msg?'':' hidden'}${type?` ${type}`:''}`}
function fmtTime(v){if(!v)return'غير محدد';try{return new Date(`2000-01-01T${v}`).toLocaleTimeString('ar-SA',{hour:'numeric',minute:'2-digit'})}catch{return v}}
function money(v){return `SAR ${Number(v||0).toFixed(2)}`}
function normalizeStatus(v){return String(v||'مسند').trim()}
function isActive(r){return ['في الطريق','وصل إلى العميل','قيد التنفيذ'].includes(normalizeStatus(r.status))}
function stepIndex(r){const s=normalizeStatus(r.status);return s==='في الطريق'?1:s==='وصل إلى العميل'?2:s==='قيد التنفيذ'?3:s==='مكتمل'?5:0}
function switchTab(tab){activeTab=tab;const todayTab=$('installationExecutionTodayTab'),currentTab=$('installationExecutionCurrentTab');todayTab?.classList.toggle('active',tab==='today');currentTab?.classList.toggle('active',tab==='current');todayTab?.setAttribute('aria-selected',String(tab==='today'));currentTab?.setAttribute('aria-selected',String(tab==='current'));$('installationExecutionTodayPanel')?.classList.toggle('hidden',tab!=='today');$('installationExecutionCurrentPanel')?.classList.toggle('hidden',tab!=='current');if(tab==='current')renderCurrent()}
function fillFilters(){const tech=$('installationExecutionTechnicianFilter'),team=$('installationExecutionTeamFilter');if(tech){const vals=[...new Set(rows.map(r=>r.technicianName).filter(Boolean))].sort((a,b)=>a.localeCompare(b,'ar'));tech.innerHTML='<option value="">كل الفنيين</option>'+vals.map(v=>`<option value="${esc(v)}">${esc(v)}</option>`).join('')}if(team){const map=new Map(rows.filter(r=>r.teamId).map(r=>[r.teamId,r.teamName||'فرقة غير مسماة']));team.innerHTML='<option value="">كل الفرق المسموح بها</option>'+[...map].sort((a,b)=>a[1].localeCompare(b[1],'ar')).map(([id,name])=>`<option value="${esc(id)}">${esc(name)}</option>`).join('')}}
function filteredToday(){const date=$('installationExecutionDateFilter')?.value||today(),tech=$('installationExecutionTechnicianFilter')?.value||'',team=$('installationExecutionTeamFilter')?.value||'';return rows.filter(r=>r.scheduledDate===date&&!isActive(r)&&!['مكتمل','ملغي'].includes(normalizeStatus(r.status))&&(!tech||r.technicianName===tech)&&(!team||r.teamId===team))}
function servicesHtml(r){return (r.services||[]).length?`<ul class="installation-card-services">${r.services.map(s=>`<li><span>${esc(s.name)}</span><strong>${Number(s.quantity||0)} ×</strong></li>`).join('')}</ul>`:'<p class="installation-card-muted">لا توجد خدمات مسجلة.</p>'}
function cardHtml(r){return `<article class="installation-today-card"><div class="installation-today-card-main"><div class="installation-today-card-head"><span class="installation-time">${esc(fmtTime(r.scheduledTime))}</span><span class="installation-request-number">${esc(r.requestNumber)}</span></div><div class="installation-customer-name">${esc(r.customerName||'عميل غير محدد')}</div><div class="installation-card-muted">${esc(r.installationAddress||'لا يوجد عنوان')}</div><div class="installation-card-muted">الفني: ${esc(r.technicianName||'غير محدد')}</div><div class="installation-card-muted">الفرقة: ${esc(r.teamName||'بدون فرقة')}</div>${servicesHtml(r)}</div><div class="installation-today-card-side"><div class="installation-metrics"><span>عدد الخدمات: <strong>${Number(r.totalServicesCount||0)}</strong></span><span>الإجمالي: <strong>${money(r.totalServicesAmount)}</strong></span></div><div class="installation-card-actions"><button class="execution-secondary-action" type="button" data-execution-details="${esc(r.id)}">التفاصيل</button><button class="execution-primary-action" type="button" data-execution-start="${esc(r.id)}">بدء التنفيذ <span class="execution-arrow">◀</span></button></div></div></article>`}
function renderToday(){const list=filteredToday(),box=$('installationExecutionTodayCards');if($('installationExecutionTodayCount'))$('installationExecutionTodayCount').textContent=String(list.length);if(!box)return;box.innerHTML=list.length?list.map(cardHtml).join(''):'<div class="execution-empty-state"><h4>لا توجد طلبات لهذا اليوم</h4><p>لا توجد طلبات متاحة ضمن الفرق والصلاحيات الحالية.</p></div>';renderPreview(list[0]||null)}
function renderPreview(r){const box=$('installationExecutionPreviewBody');if(!box)return;if(!r){box.className='execution-empty-state';box.innerHTML='<div class="execution-empty-icon">⌁</div><h4>لا يوجد طلب قيد التنفيذ حاليًا</h4><p>اختر طلبًا من القائمة واضغط «بدء التنفيذ».</p>';return}box.className='';box.innerHTML=`<div class="execution-current-summary panel"><div><h2>${esc(r.requestNumber)}</h2><div class="execution-summary-grid"><div class="execution-summary-item"><span>العميل</span><strong>${esc(r.customerName)}</strong></div><div class="execution-summary-item"><span>الموعد</span><strong>${esc(fmtTime(r.scheduledTime))}</strong></div><div class="execution-summary-item"><span>الفني</span><strong>${esc(r.technicianName||'—')}</strong></div><div class="execution-summary-item"><span>الفرقة</span><strong>${esc(r.teamName||'—')}</strong></div></div></div><div><div class="execution-summary-item"><span>العنوان</span><strong>${esc(r.installationAddress||'—')}</strong></div><p>${esc(r.customerPhone||'')}</p>${mapButton(r)}</div></div>`}
function mapButton(r){const href=r.customerMapUrl||(r.installationAddress?`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(r.installationAddress)}`:'');return href?`<a class="execution-map-btn" href="${esc(href)}" target="_blank" rel="noopener">فتح في خرائط جوجل</a>`:'<span class="installation-card-muted">لا يوجد رابط موقع مسجل</span>'}
function stepperHtml(r){const idx=stepIndex(r);const steps=[['بدء التحرك','🚗'],['فتح موقع العميل','⌖'],['وصل الموقع','📍'],['بدء التركيب','🔧'],['تم الانتهاء','✓']];return `<div class="panel execution-stepper"><h3>مسار تنفيذ الطلب</h3><div class="execution-steps">${steps.map((x,i)=>`<div class="execution-step ${i<idx?'done':''} ${i===idx?'active':''}"><div class="execution-step-circle">${x[1]}</div><div class="execution-step-label">${x[0]}</div></div>`).join('')}</div></div>`}
function nextStage(r){const s=normalizeStatus(r.status);if(s==='مسند')return{label:'بدء التحرك',next:'في الطريق',hint:'اضغط لبدء التحرك وتسجيل الوقت الحالي.',icon:'🚗'};if(s==='في الطريق')return{label:'وصلت إلى الموقع',next:'وصل إلى العميل',hint:'افتح موقع العميل ثم سجّل الوصول عند الوصول الفعلي.',icon:'📍'};if(s==='وصل إلى العميل')return{label:'بدء التركيب',next:'قيد التنفيذ',hint:'ابدأ التركيب بعد التأكد من جاهزية الموقع.',icon:'🔧'};if(s==='قيد التنفيذ')return{label:'تم الانتهاء من التركيب',next:'مكتمل',hint:'أضف الصور والملاحظات ثم اعتمد الانتهاء.',icon:'✓'};return null}
function currentHtml(r){const stage=nextStage(r);return `<div class="execution-current-shell"><div class="panel execution-current-summary"><div><h2>${esc(r.requestNumber)}</h2><div class="execution-summary-grid"><div class="execution-summary-item"><span>العميل</span><strong>${esc(r.customerName||'—')}</strong></div><div class="execution-summary-item"><span>الهاتف</span><strong>${esc(r.customerPhone||'—')}</strong></div><div class="execution-summary-item"><span>الوقت المحدد</span><strong>${esc(fmtTime(r.scheduledTime))}</strong></div><div class="execution-summary-item"><span>الخدمات</span><strong>${Number(r.totalServicesCount||0)} خدمات</strong></div></div></div><div><div class="execution-summary-item"><span>العنوان</span><strong>${esc(r.installationAddress||'—')}</strong></div><p>${esc(r.teamName||'بدون فرقة')} — ${esc(r.technicianName||'فني غير محدد')}</p>${mapButton(r)}</div></div>${stepperHtml(r)}${stage?`<div class="panel execution-current-stage"><div class="execution-stage-icon">${stage.icon}</div><div><h3>${esc(stage.label)}</h3><p>${esc(stage.hint)}</p></div><button class="execution-primary-action" type="button" data-execution-next="${esc(stage.next)}">${esc(stage.label)} <span class="execution-arrow">◀</span></button></div>`:''}<div class="panel execution-documentation"><h3>التوثيق (اختياري)</h3><div class="execution-doc-grid"><label class="execution-upload-box"><input id="installationExecutionPhotos" type="file" accept="image/jpeg,image/png,image/webp" multiple><strong>رفع صور التنفيذ</strong><small>اسحب الصور هنا أو اضغط للاختيار</small><div id="installationExecutionPhotoList" class="execution-photo-list"></div></label><label class="execution-notes-box">ملاحظات التنفيذ<textarea id="installationExecutionNotes" maxlength="2000" placeholder="اكتب ملاحظاتك هنا (اختياري)">${esc(r.executionNotes||'')}</textarea></label></div><div class="execution-completion-note">بعد إنهاء الخطوات والتوثيق ينتقل الطلب تلقائيًا إلى شاشة محاضر التركيبات.</div></div></div>`}
function renderCurrent(){const box=$('installationExecutionCurrentContent');if(!box)return;current=rows.find(isActive)||null;$('installationExecutionCurrentBadge')?.classList.toggle('hidden',!current);if(!current){box.innerHTML='<div class="panel execution-empty-state"><div class="execution-empty-icon">⌁</div><h4>لا يوجد طلب حالي</h4><p>ابدأ أحد طلبات اليوم لينتقل إلى هنا.</p></div>';return}box.innerHTML=currentHtml(current);bindPhotoInput()}
function bindPhotoInput(){const input=$('installationExecutionPhotos');if(!input)return;input.addEventListener('change',()=>{selectedFiles=[...input.files];const list=$('installationExecutionPhotoList');if(list)list.innerHTML=selectedFiles.map(f=>`<span class="execution-photo-chip">${esc(f.name)}</span>`).join('')})}
async function load(){setStatus($('installationExecutionStatus'),'جاري تحميل طلبات التنفيذ...');try{rows=await window.InstallationsService.executionWorkspace();fillFilters();if($('installationExecutionDateFilter')&&!$('installationExecutionDateFilter').value)$('installationExecutionDateFilter').value=today();renderToday();renderCurrent();setStatus($('installationExecutionStatus'),'')}catch(e){setStatus($('installationExecutionStatus'),e.message,'error')}}
async function startRequest(id){const r=rows.find(x=>x.id===id);if(!r)return;try{await window.InstallationsService.advanceExecution({id:r.id,nextStatus:'في الطريق',notes:r.executionNotes||'',photos:[]});await load();switchTab('current')}catch(e){setStatus($('installationExecutionStatus'),e.message,'error')}}
async function advance(nextStatus){if(!current)return;const btn=document.querySelector('[data-execution-next]');if(btn)btn.disabled=true;try{const notes=$('installationExecutionNotes')?.value.trim()||'';await window.InstallationsService.advanceExecution({id:current.id,nextStatus,notes,photos:nextStatus==='مكتمل'?selectedFiles:[]});selectedFiles=[];await load();if(nextStatus==='مكتمل')switchTab('today');else switchTab('current')}catch(e){setStatus($('installationExecutionCurrentStatus'),e.message,'error')}finally{if(btn)btn.disabled=false}}
document.addEventListener('DOMContentLoaded',()=>{window.addEventListener('kyum-view-changed',e=>{if(e.detail?.view==='installationExecution')load()});document.querySelectorAll('[data-execution-tab]').forEach(b=>b.addEventListener('click',()=>switchTab(b.dataset.executionTab)));$('refreshInstallationExecutionBtn')?.addEventListener('click',load);['installationExecutionTechnicianFilter','installationExecutionTeamFilter','installationExecutionDateFilter'].forEach(id=>$(id)?.addEventListener('change',renderToday));$('installationExecutionTodayCards')?.addEventListener('click',e=>{const start=e.target.closest('[data-execution-start]'),details=e.target.closest('[data-execution-details]');if(start)startRequest(start.dataset.executionStart);if(details){const r=rows.find(x=>x.id===details.dataset.executionDetails);if(r)renderPreview(r)}});$('installationExecutionCurrentContent')?.addEventListener('click',e=>{const b=e.target.closest('[data-execution-next]');if(b)advance(b.dataset.executionNext)})});
})();
