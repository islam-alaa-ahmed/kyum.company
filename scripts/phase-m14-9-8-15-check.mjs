import fs from 'node:fs';
const files={scheduling:fs.readFileSync('assets/js/installation-scheduling.js','utf8'),service:fs.readFileSync('assets/js/installations-service.js','utf8'),css:fs.readFileSync('assets/css/installation-scheduling.css','utf8'),migration:fs.readFileSync('supabase/migrations/phase_m14_9_8_15_installation_day_lock_smart_availability.sql','utf8'),version:JSON.parse(fs.readFileSync('version.json','utf8')),sw:fs.readFileSync('service-worker.js','utf8')};
const checks=[['calendar lock control',files.scheduling.includes('data-toggle-day-lock')],['locked dates disabled',files.scheduling.includes("info.isLocked?'disabled':''")],['technician booked times',files.scheduling.includes('refreshBookedTimes')],['glass lock overlay',files.css.includes('installation-day-lock-glass')],['permission edit gate',files.service.includes("requireAction('edit','installationSchedule')")],['database lock guard',files.migration.includes('guard_installation_schedule_write')],['technician conflict rpc',files.migration.includes('get_installation_technician_booked_times')],['version sync',files.version.version==='18.50.1'&&files.version.build===185001&&files.sw.includes('18-50-1')]];
let failed=0;for(const [name,ok] of checks){console.log(`${ok?'PASS':'FAIL'} ${name}`);if(!ok)failed++;}if(failed)process.exit(1);
