# Phase M14.9.8.7.2 — Quotation Conversion Visibility Consistency Recovery

Root cause: stale quotation cache and legacy unlinked requests.

Fix: canonical reverse relation fallback, cache invalidation, immediate UI refresh, and deterministic legacy link repair.
